[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build -- --configuration production"]

[start]
cmd = "npx serve dist/cristobal-frontend/browser -l $PORT --no-clipboard"
